.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Screenshots are generated by Tools/screenshots.sh. Only the contact sheet is
# tracked, because the README shows it — the 32 full-resolution captures were
# re-recorded on almost every change and put 179 MB of superseded PNGs into
# this repository's history before anyone noticed.
Screenshots/*.png
!Screenshots/contact-sheet.png
